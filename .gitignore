node_modules/
*.log
update-backup/
update-check.json
update-dismiss.json
.DS_Store

# 在仓库内直接跑 assets/scripts/*.ps1 做测试时，脚本会按自身位置推导
# 安装目录并把日志/备份写进 assets/。这些属于运行产物，不能进仓库、也不该被打包。
assets/logs/
assets/update-backup/
assets/launcher.json

# npm pack 的产物。发布时手动打包并上传到 GitHub Release，不进仓库。
*.tgz
