# Changelog

本文件记录用户可感知的变更。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.11.0] - 2026-09-23

界面上的重启/升级入口从「右上角悬浮控件」搬进了**会话标题右侧的胶囊**，
并把升级能力也放了进去。宿主侧的路由、鉴权与升级脚本编排完全不变。

> 说明：`0a27501`（垂直对齐）与 `79d713f`（两按钮差 7px）这两个提交从未发布过 ——
> 它们修的都是悬浮按钮的毛病，而悬浮按钮在这次改动里被整个删除。两处修复因此不会出现在
> 任何发布版本里；1.10.3 之后的本版就是下一个发布版本。

### 新增

* **选版卡：多个候选时自己挑升到哪个版本。** 之前只能升宿主推荐的那一个，而推荐值是
  「最保守通道里的可更新项」，**不等于最新** —— 比如 `next=0.1.7-rc.1`、`alpha=0.1.8-alpha.1`
  时推的是 rc。现在候选多于一个时，点版本按钮会先摆出一张卡片，列出 `newer` 的全部候选
  （带通道标签，按保守 → 激进排列），默认选中推荐的那个，确认后才装。
  服务端的白名单校验认的本来就是整个 `newer` 列表，所以列表里任何一个都是合法目标 ——
  这个功能只是把本来就允许的事给出入口。
* 被忽略过的候选在卡里**照样可选**（标一下「曾忽略」）。选了它提醒会自动恢复：
  装完你就是那个版本，旧版本的忽略项不再匹配。
* **会话标题右侧的「⟳ 重启 | 版本」胶囊。** 本包现在是一个**双面包**：`package.json`
  声明 `dsh.client`（`platform: web`）、导出 `./client`，浏览器半边把这两个按钮注册进
  dsh 的 `conversation.session.header.utilities` 槽 —— open-in-app 的「在文件资源管理器
  中打开」分裂按钮就在同一个槽里，所以这一簇看起来是一排原生控件。
* **版本按钮现在能一键升级**：发现新版时点它 → 二次确认 → 停服务 → `npm install` →
  校验 → 重启并回连；失败自动回滚。`allowSelfUpdate: false` 时退化成「只提醒」，
  点它只重新检查。
* 重启/升级期间有**全屏进度卡**，把「已发出请求 / 等待服务退出 / 新进程启动中 /
  正在重连」分档显示；超时或失败会写出具体原因，并可收起继续用。
* 重启完成的判据是**进程指纹（nonce）变化 + 新进程 ready**。只看 `ready` 不够：
  命令刚发出时老进程还没退场，它也会答 `ready`，只看这个会在服务真正重启前就刷新页面。
* 标签页从后台切回前台时会重新踢起被浏览器冻结的轮询，不会永远停在「启动中…」。

### 变更

* **右上角那两个悬浮控件已移除**，连同支撑它们的整套机制：`tapIndex` 注入的自包含脚本、
  悬浮按钮与更新徽标、以及 `avoidAppButtons` 那套「扫描顶栏控件 + 整簇左移让位」的
  坐标探测（约 480 行脚本 + 150 行探测逻辑）。原因是那个位置和 dsh 自己的会话头部
  右上角（分栏视图按钮）**天然重叠**，只能靠让位补偿绕开；搬进槽里之后重叠从根上消失。
  `lib/index.js` 从 60 KB 瘦到 28 KB，`avoidAppButtons` 配置项一并删除。
* 升级路径上的「忽略某个版本」不再有独立入口（原来在悬浮面板里有一排「忽略 / 恢复」按钮）。
  路由和 `update-dismiss.json` 都还在，被忽略的候选现在显示在**选版卡**里并标注「曾忽略」，
  选中它就会恢复提醒 —— 于是「忽略后进不去恢复」这个死角顺带没了。
* `update-check` 的响应新增 `canApply` 字段。没有它，前端无从判断当前部署允不允许自助
  升级，只能照样摆出「点一下就升级」的提示，点了才吃 403。
* 浏览器半边只有 `react` 一个外部依赖，并且**不需要构建工具链**：`client/build.mjs`
  用 node 内建模块把 `client/src/*.js` 合成 dsh 要求的「惰性 CJS 工厂」形状
  （`tsdown` 的 `clientBundle` preset 没有随包发布，仓库外的插件得自己复现）。
  刻意不用 `@deepseek-ai/dsh-client-ui-primitives` 等平台包 —— 它们的导出名不在本包
  能核对的契约里。
* 控制器加了**代次（generation）守卫**：一次重启/升级超时或失败后再次发起时，上一轮
  还在飞的异步回调会被丢弃，不再覆盖新动作的状态（原来会把新重启从 `calling` 打回
  `waiting`，甚至直接打成 `error`）。

### 测试

* 新增 `test/plugin-test.mjs`（33 项）：在 vm 沙箱里加载**真正会被浏览器加载的
  `lib/client.js`**，验模块契约、slot 注册、词典中英键一致、渲染结构、点击行为、
  升级流程（含「只提醒模式绝不安装」）、**选版卡（默认选中推荐项、换选后装的是选中的
  那个、取消不发请求、列表外版本被拒、选版期间不算忙）**、进度卡、重启状态机
  （含「指纹没变不许跳转」、超时收手、超时后重发、连点去重）。不引入 React / jsdom ——
  自带一层最小替身，保持本包零第三方依赖。
* 新增 `test/host-route-test.mjs`（11 项）：五条路由都注册、关掉更新检查只影响更新路由、
  没有 web 端口时什么都不挂、未登录必须 401、握手字段形状、以及 `canApply` 两种取值。
* 删除 `test/client-test.mjs`（45 项）：它全部在测已移除的悬浮按钮与顶栏避让行为，
  对宿主路由零覆盖。等量的宿主覆盖由上面那个新文件接手。
* `npm test` 现在先跑 `node client/build.mjs`，保证测的是最新的构建产物。

---

## [1.10.3] - 2026-09-22

第一个公开版本。发布前的三处必修。

### 修复

* **更新候选不再接受非 semver 的版本值（安全）**。registry 的 `dist-tags` 是**不可信输入**：
  当一个 tag 的值不是合法 semver 时，比较会退回字符串比较，于是 `https://…/x.tgz` 这类字符串
  排序上就能「比当前版本新」而混进候选；候选会一路走到升级脚本的 `npm install <包>@<值>`，
  而 npm 接受 `file:` / `https://…tgz` 形式的 spec。等于让 registry（或中间人镜像）决定
  用户机器上装什么。现在候选列表两端都要求合法 semver，「当前版本」本身读歪时直接跳过比较，
  触发升级的路由也会独立再校验一次。
* **`npm test` 不再依赖作者本机的目录**。测试里写死的绝对路径会让任何其他检出与 CI 直接报
  `ERR_MODULE_NOT_FOUND`。

### 变更

* 删除 `package.json` 里的 `dsh.compatibility`。该字段**不在 DSH 的 manifest schema 里**
  （官方只定义 `bundle` / `profile` / `client` / `configTrees` / `sessionFormatMigration` /
  `moduleFallback`），runtime 里没有任何代码读它。留着它比不写更糟——会让人误以为装到不兼容的
  版本上会被拦住。兼容环境改为在 README 里写明（实测 dsh 0.1.5-rc.1 / 0.1.5-rc.2 + Node 24 + Edge）。
* README 的安装段改写：说明为什么当前要按 tarball 直链装、为什么不能用 `github:` 形式
  （会被 pnpm 解析成 `git+ssh://`，需要 SSH key）。
* 修正 README 中的测试项数（原文写的与实际不符）。

### 测试

* 更新重试测试从 8 项增至 12 项，其中 4 项专门钉住上面那条安全修复——包括一个伪造的
  registry 响应（`latest: 'https://evil.example/x.tgz'`），断言它不会进入候选列表。

---

## 关于版本号

这个仓库从 `1.10.3` 开始公开发布，此前的版本号是私有开发期间的计数，
没有对应的 tag 与发布记录。公开发布之后按语义化版本推进。
